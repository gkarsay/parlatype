icon_sizes = ['16x16', '22x22']

foreach icon_size: icon_sizes
	icon_dir = join_paths(icon_size, 'actions')

	install_data(
		join_paths(icon_dir, 'widget-pt-waveviewer.png'),
		install_dir: join_paths(glade_pixmapdir, 'hicolor', icon_dir),
	)
endforeach

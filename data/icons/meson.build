install_data(
	join_paths('scalable', app_id + '.svg'),
	install_dir: join_paths(datadir, 'icons', 'hicolor', 'scalable', 'apps'),
)

install_data(
	join_paths('symbolic', app_id + '-symbolic.svg'),
	install_dir: join_paths(datadir, 'icons', 'hicolor', 'symbolic', 'apps'),
)